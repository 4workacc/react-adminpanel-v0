import { initState } from './initState';

export const initRed = (
    state: IRootState = initState,
    action: iRootAction
) => {
    switch (action.type) {
        default:
            return state
    }
}